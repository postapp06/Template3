/* DESCRIPTION - MOVED BELOW SEEK BAR */
.description {
  position: absolute;
  bottom: 60px; /* Positioned above bottom bar but below seek bar */
  left: 12px;
  right: 12px;
  z-index: 6;
  font-size: 14px;
  line-height: 1.4;
  background: rgba(0, 0, 0, 0.7);
  padding: 10px 12px;
  border-radius: 8px;
  margin-bottom: 4px;
  max-height: 54px; /* 3 lines */
  overflow: hidden;
  transition: max-height 0.3s ease;
}

/* SEE MORE BUTTON */
.see-more-btn {
  position: absolute;
  bottom: 35px;
  left: 12px;
  z-index: 7;
  background: rgba(0, 0, 0, 0.7);
  color: white;
  border: none;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 12px;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 4px;
}

.see-more-btn i {
  font-size: 10px;
}

/* Adjust profile and like section positioning */
.profile-section {
  bottom: 210px; /* Adjusted to make room for description */
}

.like-section {
  bottom: 210px; /* Adjusted to make room for description */
}
